"""Section-by-section analysis modules.

Each module exports the functions for one of the five report sections. The
public API is intentionally minimal — most callers just need `run_all`,
which is exposed via the top-level `olist_analytics.report` module.
"""
